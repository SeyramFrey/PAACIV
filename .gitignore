# Dependencies
node_modules/

# Next.js
.next/
out/
build/
next-env.d.ts

# Environnement / secrets
.env
.env*.local

# Companion visuel de brainstorming (superpowers)
.superpowers/

# Vercel
.vercel

# OS / éditeurs
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Claude Code : `launch.json` (définition du serveur de dev) est versionné,
# `settings.local.json` porte les autorisations d'outils propres à chaque
# poste et ne doit pas l'être.
.claude/settings.local.json
